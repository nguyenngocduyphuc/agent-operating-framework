# AOF workspace — auto-detected from marker file.
# Set this to override auto-detection (e.g., for monorepos
# where the marker is at a higher level than the working dir).
# AOF_WORKSPACE=/path/to/workspace
